<h2>Add <?php echo $this->grabEntityLabel(); ?></h2>
<form id="entity_add">

<?php $attributes = $this->grabAttributes() ?>
<?php if($attributes): ?>
    <?php foreach($attributes as $attribute): ?>
           <div class="row"><?php echo $this->model('panels/kinds')->grab($attribute['kind'], $attribute); ?></div>
     <?php endforeach; ?>
<?php else: ?>
    <h2>There are no attributes assigned to this entity.</h2>
<?php endif; ?>


    <div class="row">
<!--        <label>Attribute</label> <input type="text" name="attribute">-->


    <button onclick="
        supa.doAction('panels/eav/collection',
            {
                'entity':'<?php echo $this->getDo('entity') ?>',
                'perform': ['model','panels/eav','addData',
                    function() {
                        var data = [];
                             $('entity_add').getElements().each(function(e){
                                data[e.name] = e.value;
                            })
                        return data;
                    }
                ]
            }
        , $('panelcontent')); return false;">Add</button>

</form>